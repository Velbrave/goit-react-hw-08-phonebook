export const contact = state => state.contacts;
export const getContact = state => state.contacts.contacts;
export const getFilter = state => state.filter;
export const getError = state => state.contacts.error;
